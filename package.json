export const sanitizeInput = (input: unknown): string => {
  if (typeof input !== "string") return "";
  return input
    .replace(/<[^>]*>?/gm, "") // Strip raw HTML tags to prevent simple XSS
    .trim()
    .substring(0, 200); // Strict length limit
};
